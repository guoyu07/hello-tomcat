---
applications:
- name: hello-tomcat
  memory: 512MB
  buildpack: java_buildpack_offline
  path: build/libs/hello-tomcat-0.0.1-SNAPSHOT-all.jar
services:
  - hello-db
env:
  SPRING_PROFILES_ACTIVE: cloud